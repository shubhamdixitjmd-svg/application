import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.company.trainmaintenance",
  appName: "Train Maintenance",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
